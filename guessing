import random
secret = random.randint(1, 10)

while True:
    guess = input("Guess the number (1 to 10)! ")
    guess = int(guess)

    if guess < secret:
        print("Too low!")

    elif guess > secret:
        print("Too high!")

    else:
        print("Correct!")
        break